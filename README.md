INTELLIGENT CONTACT PLATFORM

---

This will be a set of re-usable components for the IcPlaftorm

Example:

For the use of the component
import { Button } from 'icp-components';

For applying
import 'icp-components/dist/styles.css';
